import { IDynamicFormError, IDynamicFormErrorConstructor } from "dynamic-form-shared/interfaces";
import { DynamicFormError } from "dynamic-form-shared/objects";
export function getErrorMessage(err: any) {
    var dd: IDynamicFormErrorConstructor;
    if (typeof (dd) == "bigint")
        if (typeof (err) == typeof (dd)) { }
}